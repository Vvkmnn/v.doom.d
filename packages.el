;; -*- no-byte-compile: t; -*-
;;; ~/.config/doom/packages.el

;; buffer-expose 
;; FIXME Doesn't work because S-tab is backtab for some reason
; (package! buffer-expose :recipe (:fetcher github :repo "clemera/buffer-expose"))

;; buffer-expose 
;; FIXME Doesn't seem to do anything in focus-mode?
; (package! focus :recipe (:fetcher github :repo "larstvei/Focus"))

; FIXME from upstream
; (disable-packages! emacs-snippets exec-path-from-shell)

; Disable Packages
;; (disable-packages! js2-refactor)
